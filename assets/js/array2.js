/*let valores = [0, 1, 2, 3, 4, 5, 6, 7, 8]

for(let pos = 0; pos < valores.length; pos++) {
    console.log(`a posicao ${pos} tem o valor ${valores[pos]}`)
}*/

let num = [0, 2, 4, 6, 8, 10]

for(let pos in num) {
    console.log(`a posicao ${pos} tem o valor ${num[pos]}`)
}